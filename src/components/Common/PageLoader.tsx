import type { ReactElement } from 'react';

const PageLoader = (): ReactElement => {
  return (
    <div className="loader-wrapper">
      <div className="loader_27"></div>
    </div>
  );
};

export default PageLoader;
